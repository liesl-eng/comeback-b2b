import CategoryPage from "./CategoryPage";

const Beds = () => (
  <CategoryPage
    category="Beds"
    title="Beds"
    subtitle="Beds and headboards from premium brands — inspected, in stock, and priced well below wholesale."
  />
);

export default Beds;
