import CategoryPage from "./CategoryPage";

const Dressers = () => (
  <CategoryPage
    category="Dressers"
    title="Dressers"
    subtitle="Dressers and storage from premium brands — inspected, in stock, and priced well below wholesale."
  />
);

export default Dressers;
